# MySQL
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=ops_platform

# 前端端口（默认 80）
FRONTEND_PORT=80

# 高风险入口紧急开关（默认关闭）
# 仅在完成身份认证、权限校验、审计与网络访问控制后按需开启。
ENABLE_SSH_TERMINAL=false
ENABLE_SFTP=false
ENABLE_BATCH_EXEC=false
ENABLE_SSH_KEY_MANAGEMENT=false
ENABLE_DEPLOY_WEBHOOK=false
